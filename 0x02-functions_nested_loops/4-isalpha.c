#include "main.h"

/**
 * _isalpha - entry
 *
 * @c: input from main
 *
 * Return: zilch
 */

int _isalpha(int c)
{

		int x;


	for (x = 'a'; x <= 'z'; x++)
	{
		if (c == x)
			else if (c >= 'A' && c <= 'Z')
			{
				return (1);
			}
	}
	else
		{
			return (0);
		}
