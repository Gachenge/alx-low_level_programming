#include <stdio.h>
/**
 * main - start the prog
 *
 * Return: end
 */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	for (a = 'A'; a <= 'Z'; a++)
	{
		putchar(a);
	}
		return (0);
}

