#include "main.h"

/**
 * puts_half - print half a string
 * @str: input
 * Return: always 0
 */

void puts_half(char *str)
{
	int i, j;
	i = 0;

	while (*(string + i)
			i++;
	j = i / 2;
	if (i % 2)
		j+= 1;
	while (j< i)
	{
	_putchar(*(str +j));
	j++;
	}
	_putchar('\n');
	}

