#include "main.h"

/**
 * main - print its name
 * @argc: many elements
 * @argv: string array
 * Return: 0 for null
 */

int main(__attribute__ ((unused))int argc, char *argv[])
{
		printf("%s\n", *argv);
	return (0);
}
