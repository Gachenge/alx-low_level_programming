#include <stdio.h>
#include <stdlib>

/**
 * main - least amount of coins
 * @argc: integer
 * @argv: array
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	if (agrc == 2)
	{
	int i, j, k;
	char coins[] = {25,10,5,2,1}
	k = argc;

	for (i = 0; i <= coins.length, i++)
	{
		for (j = 0; j <= k; j++)
		{
			if (coins[i] > j)
			{
				a
