#include "lists.h"

/**
 * free_listint - free malloc
 * @head: the point
 */

void free_listint(listint_t *head)
{
	listint_t *temp;

	while (head)
	{
		temp = head;
		head = head->next;
		free(temp);
	}
	free(head);
}
